# Copy oxi_json_typing to App, Logger and Client folders.
# Haven't found a more straightforward way to include a module in a python project
cp -R ../oxi_json_typing ../App
cp -R ../oxi_json_typing ../Logger
cp -R ../oxi_json_typing ../Client